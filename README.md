# LMS (H5P + NestJS + Vite/React + MongoDB)

## Structure
- `apps/backend` — NestJS API
- `apps/frontend` — Vite + React client
- `packages/contracts` — shared types/schemas between backend and frontend
